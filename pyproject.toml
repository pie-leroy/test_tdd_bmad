[project]
name = "katas-tdd-bmad-ai"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
norecursedirs = [
    ".git",
    ".tmp",
    ".pytest_cache",
    "tests/_tmp",
]
